<!DOCTYPE html>
<html>
  <head>
    <%- include('../partials/head') %>
  </head>
  <body>
    <%- include('../partials/nav') %>

    <div class="contact">

      <h2><%= contact.todo%></h2>
      <%= contact.check %>
      <div class="description"><%= contact.description %></div>
      <div class="show__opt">
        <!-- <a href="/contacts>">목록</a> -->
        <form class="show__btn" action="/contacts/<%= contact._id %>?_method=delete" method="post">
          <a class="show__del" href="#" onclick="confirm('Todo를 삭제하시겠습니까??')?this.parentElement.submit():null;">삭제</a>
        </form>
        <a class="show__btn" href="/contacts/<%= contact._id %>/edit">수정</a>

      </div>
    </div>
  </body>
</html>
